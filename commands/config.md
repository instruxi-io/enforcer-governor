---
description: Every setting the governor has, what it does, and which you have changed
argument-hint: [--why]
allowed-tools: Bash(node:*)
---
Show the governor's settings and print the result verbatim:

!`node "${CLAUDE_PLUGIN_ROOT}/src/report.mjs" config $ARGUMENTS`
